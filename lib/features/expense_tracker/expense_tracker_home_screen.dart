import 'package:flutter/material.dart';

class ExpenseTrackerHomeScreen extends StatelessWidget {
  const ExpenseTrackerHomeScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ExpenseTrackerHomeScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}