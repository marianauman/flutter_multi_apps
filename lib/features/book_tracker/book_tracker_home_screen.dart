import 'package:flutter/material.dart';

class BookTrackerHomeScreen extends StatelessWidget {
  const BookTrackerHomeScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BookTrackerHomeScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}